# This is a sample configuration file for GitHub Copilot.
# For more information, see https://github.com/github/copilot-docs/blob/main/docs/copilot/configuring-copilot.md

# Rules for excluding files from Copilot.
# You can use glob patterns to specify which files to exclude.
# For example, to exclude all files in the `dist` directory, you would use:
# exclude:
#   - "dist/**"

# Rules for including files in Copilot.
# You can use glob patterns to specify which files to include.
# For example, to include all files in the `src` directory, you would use:
# include:
#   - "src/**"
